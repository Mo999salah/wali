{
  "identifier": "settings",
  "description": "Minimal IPC for the local Settings window only.",
  "local": true,
  "windows": ["settings"],
  "permissions": [
    "allow-settings-snapshot",
    "allow-open-downloads-dir",
    "allow-update-setting"
  ]
}
